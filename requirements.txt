# OpenEyes Requirements
# Robot Vision System for Humanoid Robots

# Core (Required)
opencv-python>=4.8.0
numpy>=1.24.0
PyYAML>=6.0
python-dotenv>=1.0.0

# AI/ML (Required)
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.4.0
onnxruntime>=1.15.0
mediapipe==0.10.9
timm>=1.0.0

# Communication
pyserial>=3.5

# Development
pytest>=7.4.0
pytest-cov>=4.1.0
pylint>=3.0.0
mypy>=1.5.0
black>=23.0.0
isort>=5.12.0

# DeepStream SDK (Optional - for production pipelines)
# Install on Jetson: sudo apt install deepstream-7.1
# pip install pyds

# ROS2 (Optional - for robot integration)
# Install on Jetson: 
#   sudo apt install ros-humble-vision-msgs
#   sudo apt install ros-humble-cv-bridge
#   sudo apt install ros-humble-image-transport
#   pip install python-can
#
# cv_bridge is required for ROS2 image conversion
# Note: cv_bridge must be installed via apt, not pip
